import { paths } from "@constants";

export const walletTabs = [
    { name: 'Deposit', key: paths.accountDeposit },
    { name: 'Swap', key: paths.bcSwap },
    { name: 'Withdraw', key: paths.accountWithdraw },
]